import { SearchTagPipe } from './search-tag.pipe';

describe('SearchTagPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTagPipe();
    expect(pipe).toBeTruthy();
  });
});
